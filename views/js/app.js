/* ========================
   AEGIS' SHARED JAVASCRIPT
   ======================== */

/* Slides the side menu in/out and fades the overlay. */
function toggleMenu() {
  document.querySelector('.app').classList.toggle('menu-open');
}
